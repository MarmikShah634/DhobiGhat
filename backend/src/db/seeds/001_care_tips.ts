import type { Knex } from 'knex';

// Placeholder seed: actual wash types and price items are per-washerman
// No global data to seed in this schema.
export async function seed(_knex: Knex): Promise<void> {
  // No-op: all data is washerman-specific
}
