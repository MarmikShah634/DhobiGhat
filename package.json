{
  "name": "dhobighat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "test": "cd backend && npm run lint && npm run format:check"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
